import Serial.RXCommands.RXADDSHORTS;

public class SerialActions { //Contains methods that act on different command messages
    public static void RXADDSHORTS(RXADDSHORTS command)
    {
        //System.out.println(command.toReadableString());
    }
}
